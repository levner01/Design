你现在负责 DesignWan 项目的 S1 / GLM-M0-02「本地持久化技术 Spike」。

这不是业务功能开发，也不是 M0-03 LocalStore 实现。你的任务是用可运行、可重复、可反证的工程证据，决定加密 SQLite + FTS5 + sqlite-vec 能否成为 DesignWan 的本地持久化技术基线。

一、仓库与基线

Repository:
https://github.com/levner01/Design

M0-01 已验收基线：
branch: glm/m0-01-electron-skeleton
SHA: 6bde57e40a74da33cfcca58e5f9befa5ccb6b120

从该 SHA 创建分支：

git fetch origin
git switch -c glm/m0-02-local-persistence-spike 6bde57e40a74da33cfcca58e5f9befa5ccb6b120

不得从 main 开始，因为 main 尚未包含已验收的 M0-01 完整交付。

当前工作区可能存在未跟踪文件：
apps/browser-extension/src/background 2.ts

它不属于本任务：
- 不得修改；
- 不得删除；
- 不得暂存；
- 不得提交。

开发前先阅读：

1. docs/delivery/GLM_IMPLEMENTATION_TASKS.md 中 GLM-M0-02
2. docs/delivery/CODEX_ACCEPTANCE_MATRIX.md 中 S1 / M0-02
3. docs/delivery/HANDOFF_PROTOCOL.md
4. docs/architecture/STACK_AND_ARCHITECTURE.md 中本地数据、Key Hierarchy、M0 Spike
5. docs/architecture/DATA_MODEL.md 中 SQLite、Migration 与验收门
6. docs/delivery/GLM_M0_01_ACCEPTANCE.md 最终结论

运行环境固定：
- Node.js 24
- pnpm 11.13.1
- Electron 33
- TypeScript
- macOS arm64/x64 + Windows x64

二、任务目标

证明或否决以下组合：

1. 加密 SQLite 可在 Electron Packaged App 内真实创建、打开、关闭和恢复。
2. 正确 Key 可读，错误 Key 必须稳定失败，且不得新建、清空或覆盖原库。
3. FTS5 可在加密库中完成插入、查询、删除、重建。
4. sqlite-vec 可在加密库/候选驱动中完成加载、插入、Top-K、删除和重建。
5. 10k 固定 Fixture 可以完成性能、体积和一致性测试。
6. WAL、Busy、并发访问、进程 Kill、Migration 中断后能够恢复且 integrity_check 通过。
7. 原生模块能从 macOS arm64、macOS x64、Windows x64 的真实 Packaged App 中加载运行。
8. DB、WAL、SHM、Temp、Crash、Log 不出现敏感 Canary 明文。
9. 如果组合不可行，返回 REJECT + ADR Candidate，不得静默关闭加密/FTS/vec，不得改用云数据库。

三、允许修改范围

仅允许：

- package.json
- pnpm-lock.yaml
- pnpm-workspace.yaml（仅原生依赖 allowBuilds）
- .gitignore（仅新增 Spike 运行产物）
- apps/desktop/package.json
- apps/desktop/electron-builder.json
- apps/desktop/scripts/build.mjs（确有打包需要时）
- apps/desktop/src/main/**（仅 Packaged Spike 测试入口）
- tests/spikes/local-persistence/**
- tests/fixtures/local-persistence/**
- scripts/verify/verify-local-data.mjs
- scripts/verify/local-persistence/**
- .github/workflows/ci.yml（仅增加 S1 三平台矩阵和 Artifact）

禁止修改：

- docs/product/**
- docs/architecture/**
- docs/adr/**
- docs/delivery/**
- packages/modules/**
- packages/platform/** 的生产 LocalStore/VectorStore 实现
- Renderer、Preload、Browser Extension、Native Host
- 任何业务表、领域 Repository、业务 UI
- Supabase、云数据库、云对象存储
- M0-03/M0-04/M0-05 内容

Spike 报告允许写在：
tests/spikes/local-persistence/SPIKE_REPORT.md

不得把运行生成的数据库、密钥、原生二进制和大 Fixture 提交进 Git。

四、内部开发顺序

02-A：候选与 Harness

建立候选矩阵，至少记录：

- SQLite Driver/Binding 名称与精确版本
- 内嵌 sqlite_version()
- SQLite compile_options
- 加密机制及正确/错误 Key 行为
- FTS5 支持
- Extension Loading 支持
- sqlite-vec 精确版本
- Electron ABI/N-API
- macOS arm64/x64、Windows x64 原生二进制来源
- ASAR/asarUnpack 策略
- 开源许可证、归属要求、商业/Trial 限制
- 依赖维护状态、升级风险、Artifact 体积
- 主候选与备选候选

所有关键依赖必须精确锁版本，禁止使用 ^、*、latest。

建议 Commit：
spike: scaffold persistence decision harness

02-B：加密 SQLite、Migration 与 Crash

实现最小技术表，不得使用业务实体：

- spike_meta
- spike_documents
- spike_migrations

必须覆盖：

1. 正确 Key 创建和重开。
2. 错误 Key 稳定失败。
3. 错误 Key 不得新建、截断或覆盖原库。
4. 原始 DB Header 不得出现 SQLite format 3。
5. DB/WAL/SHM/Temp/Log 不得出现固定敏感 Canary。
6. Foreign Keys、WAL、Busy Timeout。
7. v0→v1→v2 最小 Migration。
8. Migration 中途强杀，重启后只能是旧版本或完整新版本，禁止半状态。
9. 事务中途 Kill，已提交数据保留，未提交数据不可见。
10. 1 个写入口 + 8 个并发读者压力；不得丢写或损坏。
11. quick_check/integrity_check 通过。
12. Crash Recovery 时间和 WAL 大小写入结果报告。

必须输出底层 SQLite 版本。若使用多连接 WAL，而 SQLite 属于已知脆弱版本且无法证明包含官方修复/回补，立即 Partial Handoff，不得继续伪装通过。

建议 Commit：
spike: prove encrypted sqlite recovery

02-C：FTS5、sqlite-vec 与删除

使用确定性 10,000 条 Fixture，包含：

- 中文文本
- 英文文本
- 混合文本
- 固定 768 维向量
- 固定 ID 和 Seed
- 明确的可删除集合

FTS5 必须证明：

- 创建
- 批量插入
- 中文/英文/混合查询
- 更新
- 删除
- rebuild/optimize
- 删除后命中为 0

sqlite-vec 必须证明：

- 原生 Extension 在当前 SQLite 连接中真实加载
- 输出 sqlite-vec 版本
- vec0 插入
- Top-K 查询
- 过滤
- 删除
- 100% 清空
- 重建
- 删除后旧 ID 不得再次出现

建立独立 Exact-search Oracle。对冻结 Fixture 的 Top-10 结果执行一致性比较；tie 必须有确定性处理，不能用模糊日志宣称“一致”。

sqlite-vec 的表结构、SQL、RowID 和 Extension 路径只能存在于 Spike 内，不能进入业务 Module 或 Public Interface。

建议 Commit：
spike: prove local search lifecycle

02-D：负例、取证与性能门

verify:local-data.mjs 必须区分两种模式：

1. pnpm verify:local-data -- --spike
   - 运行真实 M0-02 Spike
   - 全部成功才 exit 0

2. pnpm verify:local-data
   - 继续明确 exit 1
   - 提示完整 LocalStore/ObjectStore/VectorIndex 尚待 M0-03
   - 不得因为 Spike 通过而假装生产实现已经完成

不得提供 DISABLE、SKIP 或 CI-only 假绿开关。

为每种失败输出唯一 Reason Code，至少包含：

- PERSISTENCE_SPIKE_DRIVER_LOAD_FAILED
- PERSISTENCE_SPIKE_ENCRYPTION_DISABLED
- PERSISTENCE_SPIKE_WRONG_KEY_ACCEPTED
- PERSISTENCE_SPIKE_MIGRATION_CORRUPT
- PERSISTENCE_SPIKE_CRASH_RECOVERY_FAILED
- PERSISTENCE_SPIKE_FTS_DELETE_FAILED
- PERSISTENCE_SPIKE_VEC_LOAD_FAILED
- PERSISTENCE_SPIKE_VEC_DELETE_FAILED
- PERSISTENCE_SPIKE_PLAINTEXT_LEAK
- PERSISTENCE_SPIKE_PACKAGED_LOAD_FAILED
- PERSISTENCE_SPIKE_LICENSE_BLOCKED

负例必须精确断言：

- exit code
- Reason Code
- 被注入故障
- 恢复后正向测试重新 PASS

最低 Spike Guardrail（不是最终产品 SLO）：

- 10k Fixture 构建不超过 120 秒
- FTS Top-20，100 次查询后报告 p50/p95，p95 目标 ≤250ms
- Vector Top-10，100 次查询后报告 p50/p95，p95 目标 ≤250ms
- Crash Recovery 目标 ≤5 秒
- 报告 DB、WAL、扩展与最终安装包增量体积

如果超过目标，不得隐瞒；记录平台、硬件、差距和优化/替代候选，交 Codex 做 Go/No-Go。

建议 Commit：
test: harden persistence spike gates

02-E：真实 Packaged App 与远端 CI

不得只在 node tests、electron .、源码目录中通过。

必须让 Electron Packaged App 的 Main 进程真实执行 Spike，并通过安全 Sentinel 返回结构化结果。

测试入口必须双开关限制，只能用于验收模式；生产普通启动不得自动运行 Spike，也不得暴露给 Renderer/Preload。

在 .github/workflows/ci.yml 增加独立 S1 Matrix：

- macos-15 / arm64
- macos-15-intel / x64
- windows-latest / x64

每个 Job 必须：

1. Node 24 + pnpm 11 冷安装。
2. frozen-lockfile。
3. TypeScript typecheck。
4. 无缓存 Build。
5. pnpm verify:quick。
6. pnpm verify:local-data -- --spike。
7. 只构建当前 Matrix 架构。
8. 从真实 Packaged App 内重复执行核心 Spike。
9. 校验目标可执行文件和原生模块架构。
10. 上传安装包、Spike JSON、版本清单、性能/体积结果。
11. Artifact 缺失时必须 error。

Artifact 名称：

- local-persistence-spike-mac-arm64
- local-persistence-spike-mac-x64
- local-persistence-spike-win-x64

保留现有 M0-01 验收 Job和“未实现门必须失败”Job。后者调用不带 --spike 的 verify:local-data，仍应 exit 1。

建议 Commit：
ci: prove packaged persistence matrix

五、立即停工条件

出现以下任何情况，不得继续后续实现，立即返回 Partial Handoff：

1. 主候选需要更换 SQLite、加密、Vector 或顶层架构。
2. macOS/Windows 任一目标平台不能加载原生模块。
3. 错误 Key 能读取数据或破坏原库。
4. DB、WAL、SHM、Temp、Crash、Log 出现 Canary 明文。
5. Migration 或 Crash Recovery 产生不可恢复半状态。
6. sqlite-vec 只能通过开发态运行，无法从 Packaged App 加载。
7. 需要静默禁用加密、FTS5 或 Vector 才能通过。
8. 许可证不允许计划中的闭源 Beta 分发，或只能依赖 Trial。
9. 需要将业务事实源改为 Supabase/云数据库。
10. 需要修改允许目录之外的文件。

Partial Handoff 必须提供：

- 最后一个可复现 Commit
- 失败命令和完整日志
- macOS/Windows 影响
- Candidate A/B 对比
- 许可证与成本影响
- Migration/删除影响
- 推荐 ADR
- 回滚点

不得自行修改 docs/adr 或架构基线。

六、提交与交付规则

内部按上述五个 Commit 推进，不需要每个 Commit 都等待 Codex 验收。

每个 Commit 后自行运行：

corepack pnpm exec tsc -b --force
corepack pnpm verify:quick
corepack pnpm verify:local-data -- --spike

失败不得继续依赖它的下一步。

全部完成后：

1. Push glm/m0-02-local-persistence-spike。
2. 等待三平台 CI 全部完成。
3. 不合并 main。
4. 不创建 PR，除非凯哥明确要求。
5. 一次性提交完整 Handoff，等待 Codex 统一验收。

交付格式：

阶段：S1
任务卡：GLM-M0-02
基线 SHA：6bde57e40a74da33cfcca58e5f9befa5ccb6b120
各内部 Commit：
最终交付 SHA：
分支：
CI Run URL：

1. 主候选与备选结论
2. 精确依赖、SQLite/sqlite-vec/Electron/ABI/许可证清单
3. 修改文件与理由
4. 加密、错误 Key、Migration、WAL、Crash 结果
5. FTS5、vec、Exact Oracle、删除结果
6. 明文扫描结果
7. 10k 性能与体积结果
8. macOS arm64/x64、Windows x64 Packaged App 结果
9. Artifact 名称、大小、下载地址
10. 所有测试命令与精确退出码
11. 未完成项、风险和偏差
12. 最终建议：PASS Candidate 或 REJECT + ADR

提交前最后检查：

git status --short
git diff --cached --name-only

确认没有提交：
apps/browser-extension/src/background 2.ts
任何 docs/**
任何生成数据库、密钥、Fixture 大文件或原生二进制